{
  // GENERATED SHAPE — keep in sync with wrangler.jsonc. See test/config-parity.test.ts.
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "kompass",
  "main": "src/worker/index.ts",
  "compatibility_date": "2026-07-01",
  "observability": {
    "enabled": true,
  },
  "vars": {
    // Not secret (account IDs/namespace IDs aren't sensitive) — power the
    // /status Cloudflare-utilization panel (src/worker/cf-usage.ts).
    "CLOUDFLARE_ACCOUNT_ID": "",
    "CLOUDFLARE_KV_NAMESPACE_ID": "",
  },
  "kv_namespaces": [
    {
      "binding": "CONFIG",
      "id": "ac8f63619364493897213cd0d58feefd",
    },
  ],
  "durable_objects": {
    "bindings": [
      {
        "name": "KOMPASS_STATE",
        "class_name": "KompassState",
      },
    ],
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["KompassState"],
    },
  ],
  "triggers": {
    // Daily model-discovery run (src/worker/discovery.ts) — detect-only, never
    // mutates config. 06:00 UTC: cheap (models-list calls, no completion tokens).
    "crons": ["0 6 * * *"],
  },
}
